this string uses single quotes and can contain "double quotes" and escaped 'single quotes'
this string uses single quotes and can contain 'single quotes' and escaped "double quotes"
this string uses double brackets and can contain both "double quotes" and 'single quotes' unescaped, as well as [single brackets]. It cannot contain double closed brackets, but it can contain unescaped
newlines
this string uses super long brackets and can contain "double quotes", 'single quotes', and a variety of closing brackets:
- ]
- ]]
- ]=]
- ]==]
- ]===]
It can also contain an open bracket as long as the starting one: [====[, but not a closing bracket as long as the starting one.
